<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kusovaya">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Kusovaya">
        <activity
            android:name=".activities.addRecord.AddRecordActivity"
            android:exported="false"
            android:label="Добавить запись"
            android:windowSoftInputMode="adjustPan"/>
        <activity
            android:name=".activities.counterHistory.CounterHistory"
            android:exported="false"
            android:label="История показаний"/>
        <activity android:name=".activities.addCounter.AddCounterActivity"
            android:label="Добавить счетчик"/>
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>